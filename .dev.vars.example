# Zkopíruj na `.dev.vars` a vyplň reálnou hodnotou (soubor .dev.vars je v .gitignore).
# Návod na získání klíče: docs/BUILD.md (Brave Search API).
# Bez této hodnoty appka funguje taky — jen se nepočítá stopa na webu.

BRAVE_API_KEY=zde-tvuj-brave-search-api-token
